[build]
  command = "npm run build"
  publish = "dist"

# Proxy API to your Render backend
[[redirects]]
  from = "/api/*"
  to = "https://backend-test-xfo3.onrender.com"
  status = 200
  force = true

# Single-page app fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
